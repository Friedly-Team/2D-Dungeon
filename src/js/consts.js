export const MODES = {
  game: {
    name: "game",
    active: true,
  },
  editor: {
    name: "editor",
    active: true,
  },
};

export const EditorDefaultSettings = {
  sWidth: 512,
  sHeight: 512,
  xTotal: 32,
  yTotal: 32,
  bSize: 16,
}

export const scale = 32;
export const xTotal = 32;
export const yTotal = 21;

export const w = scale * xTotal;
export const h = scale * yTotal;

export const FPS = 30;
